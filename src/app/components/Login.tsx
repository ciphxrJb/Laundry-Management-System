'use client';

export function Login() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50 p-4">
      <div className="max-w-md text-center rounded-2xl border bg-white p-10 shadow-sm">
        <h1 className="text-3xl font-bold mb-4">Login Disabled</h1>
        <p className="text-gray-600">
          Authentication is currently disabled while we rebuild the app baseline.
          Please use the dashboard route directly for now.
        </p>
      </div>
    </div>
  );
}
